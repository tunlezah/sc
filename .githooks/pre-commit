#!/usr/bin/env bash
# Pre-commit hook: check formatting and linting
set -e

echo "Running cargo fmt --check..."
cargo fmt --all --check

echo "Running cargo clippy..."
cargo clippy --all-targets --all-features -- -D warnings

echo "Pre-commit checks passed."
